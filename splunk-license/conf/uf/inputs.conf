# The forwarder path, stated: a Universal Forwarder monitoring files on disk and
# forwarding over S2S to the indexer on 9997. Both arms travel this same path;
# the only difference between them is whether the 10x Receiver ran first.

[monitor:///data/base/otel-sample-200mb.log]
index = tenx_base
sourcetype = tenx_raw_json
disabled = false

[monitor:///data/compact/encoded.log]
index = tenx_enc
sourcetype = tenx_encoded
disabled = false

[monitor:///data/compact/templates.json]
index = tenx_dml
sourcetype = tenx_dml_raw_json
disabled = false
