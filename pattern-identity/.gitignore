# public sample + LogHub sets — fetched by bench/data.py and bench/coverage.py, never vendored
data/

# python
.venv/
__pycache__/
*.pyc

# the fork virtualenv, built by hand for the drain3-improved rerun
.venv-fork/
