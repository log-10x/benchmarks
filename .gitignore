# external datasets — each benchmark fetches its own (see per-benchmark README)
**/loghub/

# generated per-run outputs (committed reference results live in each bench/facts.json)
**/bench/out/
**/bench/stability/*
# ... except the committed per-dataset stability results (small JSON)
!**/bench/stability/stability_*.json

# python
__pycache__/
*.pyc
.venv/
venv/

# os / editor
.DS_Store
Thumbs.db
